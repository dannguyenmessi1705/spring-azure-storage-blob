azure:
  storage:
    container:
      name: files
    connection:
      string: DefaultEndpointsProtocol=https;AccountName=testjavablob;AccountKey=B9FPwah4NV7mvnOCDu6jscl538Ty2MxGxOzS3e9vALkkb+Mhf/R6nty8v+YL28dQiimUMX/p+O7N+AStJaPsng==;EndpointSuffix=core.windows.net
    account:
      name: testjavablob
    key: B9FPwah4NV7mvnOCDu6jscl538Ty2MxGxOzS3e9vALkkb+Mhf/R6nty8v+YL28dQiimUMX/p+O7N+AStJaPsng==

server:
  port: 8088

# Kết nối mySQL
# Docker và Localhost
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/spring_azure_blob
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:17052002}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    generate-ddl: true
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

## JWT
jwt:
  secret: nguyendidan